## 概要

正しい括弧列のランダム生成

## 詳細なドキュメント

#### random_bracket_sequence

```cpp
string random_bracket_sequence(int n)
```

長さ $2n$ の正しい括弧列をほぼ一様ランダムに生成する。

各文字を置いた後に正しい括弧列を作れる場合の数から遷移確率を求め、その確率に従って次の文字を選ぶ。確率の計算には `double` を用いる。

返り値は `'('` と `')'` からなる文字列。

##### 制約

- $n \geq 0$

##### 計算量

- $O(n)$

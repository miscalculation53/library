## 概要

桁和を求める。ある程度の大きさまでテーブルを作っておくことで、ループ回数が減って定数倍がよくなる。

## 詳細なドキュメント

#### digit_sum

```cpp
ll digit_sum<T>(T x)
```

非負整数 `x` の十進表記の桁和を返す。

##### 制約

- `T` は整数型
- $x \geq 0$

##### 計算量

- $O(\log x)$
  - $10^5$ 程度を前計算

# ライブラリ全体の共通方針

この方針は `library` ディレクトリ以下のすべてのライブラリに適用する。

## 整数型とオーバーフロー

- 時間・メモリの面で実行可能な入力規模を前提とする。配列長や添字が型の上限に達する以前に、時間やメモリが到底足りなくなる場合、そのオーバーフローだけを防ぐ判定や `assert` は設けない。既存の同種の判定も削除してよい。
- 実行可能な入力でも中間計算がオーバーフローし得る場合は、適切な型や計算方法を選ぶ。型の選択の問題を、利用者への追加制約や上限チェックだけで済ませない。
- 数学的な前提、添字の有効範囲、列挙の終了条件など、アルゴリズムの正しさに必要な判定は残す。大きい整数を少ない時間・メモリで扱える処理には、上記の入力規模の仮定を当てはめない。

## ドキュメント

- 形式は [docs/STYLE.md](docs/STYLE.md) に従う。
- 配列長の `<= INT_MAX` など、上記の実行可能な入力規模の仮定や引数の型から明らかな上限は制約に書かない。
- 数学的な前提や、利用者が選ぶ係数型などに関する実質的な条件を記載する。
